%% Generated from useful-claude-add-ons@a573ca24 on 2026-09-13. Verify before trusting.
%% Anchors: plugin/crew/hooks/scripts/crew_config.py, plugin/crew/hooks/scripts/crew_state.py, plugin/crew/skills/crew-graph/scripts/crew_upgrade.py
%% How one config value reaches a run, and what may set it where.
%% --
%% Drawn because nothing diagrammed it and it took four separate reads of
%% crew_config.py to pin down. Every claim below was derived by EXECUTING the
%% functions, not by reading their comments -- the counts (86/45/41) come from
%% leaf_paths(), and the refusals from is_global_path(). The prose companion is
%% plugin/crew/CONFIG.md, which carries the per-key tables this cannot.
%% --
%% Re-derived at a573ca24 against e245aa0d. Four of the five anchored paths had
%% changed. What moved: schema 4 -> 5 added install.policy, which is globally
%% settable, so BOTH totals rose by one (85->86, 44->45) and the repo-only
%% difference did not move at all. That one key did two more things this diagram
%% had to grow a subgraph for each:
%%   - it turned the single-key ratchet in the WRITE path into a TABLE
%%     (_RATCHETED), so "widens_authority" no longer exists by that name;
%%   - it is the ONE key for which "repo beats global" is FALSE. It resolves to
%%     the LOWER-ranked layer. A diagram whose subject is precedence cannot
%%     carry an unmarked exception to it -- see the "does NOT follow" subgraph.
%% Every line number in this file was re-taken by AST at this anchor; none was
%% offset. The ones that did not move are marked "unmoved" so a reader can tell
%% a re-derived number from a carried one.
%% --
%% The one sentence this diagram exists to make visible:
%%   what the global file may WRITE is exactly what the global layer may SUPPLY,
%%   because both rules are enforced against default_global_config().
%% The READ side prunes against it (filter_global); the WRITE side refuses
%% paths against it (plan_global_write). One definition, two directions.

flowchart TB

    subgraph Sources["Three sources, lowest precedence first"]
        direction TB
        DEF["default_config()<br/>crew_config.py:270 <i>(unmoved)</i><br/><b>86 leaves</b><br/>composed from crew_state's<br/>PM_DEFAULTS :975, QA_DEFAULTS :1022,<br/>DEV_DEFAULTS :1031, WORKTREE_DEFAULTS :1058,<br/>INSTALL_DEFAULTS :942<br/>and crew_upgrade's DOCS_BLOCK :107 / BITBUCKET_BLOCK :150"]
        GLB[("~/.claude/crew/config.json<br/>read_global_config() :666<br/><i>never raises</i> - absent, malformed or<br/>non-object all return {}, because this is<br/>reached from a SessionStart hook")]
        REPO[(".crew/config.json<br/>crew_state.load_config() :142 <i>(unmoved)</i>")]
    end

    subgraph Gate["The global layer is filtered BEFORE it is merged"]
        direction TB
        TMPL["default_global_config()<br/>crew_config.py:383<br/><b>45 leaves</b> - the single definition<br/>both rules below are enforced against"]
        PRUNE["_prune() :598<br/>keeps only keys present in the template.<br/>Descends structurally, so naming a block<br/>grants only the leaves under it.<br/>Reports the FIRST ABSENT SEGMENT with its<br/>full path prefix - a bare block name appears<br/>only when the prefix is empty."]
        FILT["filter_global() :624<br/>returns (kept, ignored)"]
    end

    subgraph Merge["resolve_config() :719 - the single resolver"]
        direction TB
        NULLS["without_null_shadows() :575<br/>drops a repo <i>null</i> ONLY where the global<br/>layer supplies a real value (_layer_supplies :863).<br/>Narrow on purpose: context.reserveTokens: null<br/>still means <b>off</b>."]
        M1["merge_defaults(default_config(), global)<br/>crew_state.py:1417"]
        M2["merge_defaults(that, repo)"]
        SCHEMA{"schema<br/>lifted OUT of the merge"}
        OUT(["resolved config<br/><i>repo beats global beats default</i>"])
    end

    DEF --> M1
    GLB --> FILT
    TMPL -.->|"the shape to prune to"| PRUNE
    PRUNE --> FILT
    FILT -->|kept| M1
    FILT -.->|"ignored - named, never silent;<br/>inspect_global :962 reports them"| IGN>"keys a global file may not set"]
    REPO --> NULLS
    NULLS --> M2
    M1 --> M2
    M2 --> SCHEMA
    SCHEMA -->|"in repo file: use it"| OUT
    SCHEMA -->|"absent: pop it entirely"| OUT

    subgraph Write["The WRITE side - /crew:config --set"]
        direction TB
        PLAN["plan_global_write() :1510<br/><i>pure</i>; returns (merged, changes)"]
        ISG{"is_global_path() :647"}
        REFUSE>"GlobalWriteRefused<br/>names the path AND lists every allowed one"]
        VAL{"validate_providers() :171 <i>(unmoved)</i>"}
        PERR>"ProviderError<br/>a VALUE check, not a path check"]
        MERGEW["merge, never replace<br/>keys the file already holds that<br/>the update does not name survive<br/>byte-for-byte, including keys this<br/>module has never heard of"]
        WIDEN["_widens() :1473<br/>renamed from widens_authority: the ratchet<br/>is a TABLE now, _RATCHETED :1496, and a key<br/>not in it never widens.<br/>Rank, NOT equality - act to autonomous<br/>must not ship unannounced, and<br/>autonomous to act must not warn."]
        WRITE["write_global_config() :1597<br/>the only function in crew that<br/>writes outside the repo"]
    end

    PLAN --> ISG
    ISG -->|"not in the template"| REFUSE
    ISG -->|allowed| VAL
    VAL -->|"bad provider name"| PERR
    VAL -->|ok| MERGEW
    MERGEW --> WIDEN
    WIDEN --> WRITE
    WRITE --> GLB
    TMPL -.->|"the SAME object the read side prunes to"| ISG

    subgraph Ratchet["_RATCHETED :1496 - two keys, one mechanism"]
        direction TB
        R1["<b>pm.authority</b><br/>crew_state.authority_rank :1260<br/>normalise_authority :1246<br/>notes: _WIDENING_NOTES :1444"]
        R2["<b>install.policy</b><br/>crew_state.install_policy_rank :1290<br/>normalise_install_policy :1272<br/>notes: _INSTALL_WIDENING_NOTES :1427"]
        R3["Why a table and not a second flag:<br/>the old name promised ONE key. A sibling<br/>boolean beside it is how two mechanisms that<br/>must agree start disagreeing - so the second<br/>ratcheted key generalised the first instead<br/>of standing next to it."]
        R1 --- R2 --- R3
    end

    WIDEN -.-> Ratchet

    subgraph Asym["The asymmetry, stated because 'exactly what' hides it"]
        direction TB
        A1["Both rules agree on which PATHS."]
        A2["The WRITE path additionally rejects VALUES<br/>the READ path only reports."]
        A3["resolve_config NEVER raises - a malformed file<br/>must not wedge every session on the machine.<br/>provider_problems() :692 reports instead."]
        A1 --- A2 --- A3
    end

    subgraph Split["What may be set where - measured, not asserted"]
        direction TB
        S1["<b>45 global-settable</b> leaves, in eleven blocks:<br/>qa, dev, worktree, secondOpinion, memory, notify,<br/>pm, docs, install, bitbucket.mergeGate,<br/>and context.autoClear"]
        S2["<b>41 repo-only</b>: schema, tier, roles, tracker, jira,<br/>sdp, obsidian, verifyGate, platform, graph,<br/>emergency, and every context.* key outside autoClear"]
        S3["<b>Open tables.</b> leaf_paths() :513 treats an EMPTY<br/>DICT as a leaf, so qa.roles and dev.roles admit a<br/>role name this release has never heard of."]
        S4["<b>Consent is not capability.</b><br/>context.autoClear.unsafeFocus, declared at<br/>AUTOCLEAR_CONSENT_KEYS :267 <i>(unmoved)</i>, is excluded<br/>from the global template on purpose: one true set<br/>once would accept blind keystroke injection<br/>for every repo on the machine.<br/>The precedent is graph.obsidian.confirmed,<br/>argued in plan_global_write's docstring - that<br/>key was REMOVED from default_config() in<br/>0.16.13 and survives only as the reasoning."]
    end

    OUT -.-> Split

    subgraph Except["install.policy does NOT follow the precedence above"]
        direction TB
        E1["<b>This is the one key where 'repo beats global' is<br/>false, and the diagram would be WRONG without it.</b><br/>CONFIG.md section 15, line 845."]
        E2["The effective value is the <b>lower-ranked of the<br/>two layers</b> - crew_state.effective_install_policy :1301,<br/>reached through crew_config.resolve_install_policy :761.<br/>Neither layer can widen what the other allows."]
        E3["Why: crew reads config out of CLONED repositories.<br/>A repo shipping install.policy: auto would, under<br/>precedence, override a machine owner who chose<br/>manual - and crew would start running commands<br/>because of a file the user never wrote."]
        E4["Absent counts as manual on either side, so<br/><b>auto requires BOTH layers to say auto</b>.<br/>Setting it in a repo alone does nothing, and<br/>--install-plan names the layer holding it down -<br/>a value that quietly does nothing is worse than<br/>one refused out loud."]
        E5["auto is safe only because no repo-controlled<br/>string can become a command:<br/>crew_state.INSTALLABLE :965 maps a name to a<br/>literal argv tuple, and install_plan :1324<br/>consults that table BEFORE the policy - so an<br/>unknown name gets a null command at EVERY policy.<br/>Argv tuples, never shell strings.<br/>Both asserted in tests/test_install_policy.py."]
        E1 --- E2 --- E3 --- E4 --- E5
    end

    OUT -.-> Except
    WIDEN -.->|"ratcheted here, but resolved by RANK not precedence"| Except

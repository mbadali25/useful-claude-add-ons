%% Generated from useful-claude-add-ons@2bb92f32 on 2026-09-26. Verify before trusting.
%% Anchors: plugin/crew/hooks/scripts/crew_config.py, plugin/crew/hooks/scripts/crew_state.py, plugin/crew/hooks/scripts/crew_guards.py, plugin/crew/hooks/scripts/role_write_guard.py, plugin/crew/hooks/scripts/role-write-guard.sh, plugin/crew/hooks/scripts/crew_autoclear_setup.py, plugin/crew/hooks/scripts/crew_autocycle.py, plugin/crew/hooks/scripts/crew_migrate.py, plugin/crew/hooks/scripts/crew_context.py, plugin/crew/skills/crew-graph/scripts/crew_upgrade.py, plugin/crew/CONFIG.md, plugin/crew/tests/test_install_policy.py, CLAUDE.md
%% Re-anchored 6c497a14 -> f2bb919b (T-0015): `git diff --name-only 6c497a14 f2bb919b --`
%% over all 13 paths on the Anchors line returns nothing, so nothing below was redrawn or
%% re-read. The 're-read at 6c497a14' statements below remain true of f2bb919b by that diff.
%% Re-anchored f2bb919b -> 6d35ef8c (T-0006, crew 1.0.40): of the Anchors paths,
%% crew_config.py, crew_state.py, crew_context.py and CONFIG.md changed. Every crew_config.py
%% and crew_state.py line cited below was re-mapped by a line diff and re-read with grep -n at
%% 6d35ef8c (e.g. read_global_config :680 -> :689, _RATCHETED :2342 -> :2351, PM_DEFAULTS
%% :1079 -> :1089); the leaf counts were re-executed (117 / 66 / 51, fourth figure 0). The
%% new `resume` block is machine-global in the template like autoClear.enabled, and is enforced
%% by crew_resume.settings (plugin/crew/hooks/scripts/crew_resume.py:158), not resolve_config.
%% crew_context.py's cited lines did not move; CONFIG.md is cited by name only.
%% Re-anchored 6d35ef8c -> 2bb92f32 (T-0006 review round 3): of the Anchors paths only CONFIG.md
%% changed (auto-resume prose and the 1.0.40 key-count label, 66 re-measured), and it is cited
%% by section only; crew_resume.settings moved :157 -> :158 (a comment line above it), re-read.
%% How one config value reaches a run, and what may set it where.
%% --
%% RE-ANCHORED after crew 1.0 (previously 60c79407). Every function/line cited
%% below was re-read at 6c497a14 and 400636f5 is byte-identical to 6c497a14 for
%% every cited file (`git diff --name-only 6c497a14..400636f5 -- <cited files>`
%% returns nothing), so that pass anchored this diagram at 6c497a14 rather than
%% the worktree's own HEAD. SUPERSEDED: T-0015 re-anchored it to f2bb919b (header,
%% :1 and :3-5); 6c497a14 is where the claims below were last re-read. Of the 8 files the PREVIOUS diagram anchored on, 7
%% changed between 60c79407 and 6c497a14 - only CLAUDE.md did not (its hook
%% rule, ":43-44", still reads the same). Three modules cited below did not
%% exist at 60c79407 at all: crew_autoclear_setup.py, crew_migrate.py,
%% crew_context.py (confirmed absent with `git cat-file -e 60c79407:<path>`).
%% --
%% WHAT CREW 1.0 CHANGED IN THIS FLOW, in the order the sections below draw it:
%%  1. A FOURTH guard vocabulary, `cloudGuard` - same words as `roleWrites`
%%     (off/report/block), its own switch. `crew_guards.ALL_GUARD_NAMES` is now
%%     TEN names (six GUARD_NAMES + two PROD_GUARD_NAMES + roleWrites +
%%     cloudGuard), all ten present as leaves in BOTH default_config() and
%%     default_global_config() - confirmed by executing leaf_paths() in this
%%     worktree, not assumed from the tuple.
%%  2. The ratchet table grew again: write-side crew_config._RATCHETED is now
%%     13 keys, read-side crew_guards.RATCHETED_KEYS is 12 - executed and
%%     diffed (`set(_RATCHETED) - set(RATCHETED_KEYS)`), and the ONLY
%%     difference is still `pm.authority`, exactly the asymmetry the previous
%%     anchor drew at a smaller size. The tuple SHAPES still disagree
%%     ((function, function, dict) write-side vs (tuple, function, function)
%%     read-side), confirmed by `type()` on each element, not asserted.
%%  3. context.autoClear.enabled/onlyRepos/onlySessions are now MACHINE-GLOBAL
%%     ONLY, and the enforcement is NOT in crew_config.resolve_config at all -
%%     it lives in crew_autocycle.settings() (crew_autocycle.py:137-178),
%%     which reads both raw layers itself and computes
%%     `machine.get("enabled") is True and repo.get("enabled") is not False`
%%     (:175) - a repo may turn auto-clear OFF (`enabled: false`) and never ON.
%%     onlyRepos/onlySessions are read from the machine layer ONLY (:177-178);
%%     a repo's own copy of either is never consulted, though it IS a
%%     global-settable leaf (see the Split section) and so is visible to
%%     `/crew:config --show` under crew_config.py:1528-1529's
%%     AUTOCLEAR_MACHINE_ONLY_KEYS reporting rule.
%%  4. crew_autoclear_setup.py is the ONLY writer of `context.autoClear.*`
%%     into the global file, and it does NOT reimplement the write: it calls
%%     crew_config.write_global_config() (:2530, pre-existing sibling-then-
%%     rename with fsync, unchanged since 0.16.7 - NOT new at this anchor).
%%     What IS new is `_require_global_readable` (:107-114), called BEFORE
%%     every write, which raises `GlobalConfigUnreadable` when the global
%%     file exists but is not valid JSON/an object - so a malformed global
%%     file is refused rather than silently replaced with only the two keys
%%     this flow proposes (`_global_file_problem`, :69-104, is deliberately
%%     NOT crew_config.read_global_config, whose {} collapse is correct for a
%%     read and wrong for a write per its own docstring).
%%  5. AN OPEN AUTHORITY QUESTION, drawn because the code disagrees with
%%     itself, not because this diagram resolves it: `crew_migrate.py --apply`
%%     is the only writer of `.crew/crew.json` (schema 1); crew_config.py
%%     (the ratchet, the guards, verify-gate.sh, /crew:config, /crew:model)
%%     reads ONLY `.crew/config.json` (schema 7, crew_config.py:1 docstring);
%%     crew_context.py's load_crew_config (:121-132) reads `crew.json` FIRST
%%     and falls back to `config.json` only if `crew.json` is absent. A repo
%%     that has run --apply has both files, and which one governs a given
%%     read depends on which module asked. Labelled OPEN below; deciding which
%%     file should win is scribe's decision to record, not this diagram's.
%%  6. role-write-guard.sh's no-python fallback (:301-346) is a NEW, narrower
%%     contract, not the old policy-reading fallback (removed): without a
%%     working python it cannot evaluate guards.roleWrites at all, so it only
%%     tells a restricted role from an unrestricted one and fails CLOSED for
%%     the restricted side - "off"/"report" and pm's own path allowances
%%     require python to take effect; a restricted role's write is ALWAYS
%%     blocked with no python on PATH.
%% --
%% METHODOLOGY: leaf counts below (117 / 66 / 51 at 6d35ef8c; 116 / 65 / 51 before
%% T-0006 added `resume.auto` to both templates) come from executing
%% `crew_config.leaf_paths(crew_config.default_config())` and the same for
%% `default_global_config()` in THIS worktree (module resolution confirmed
%% local: `crew_config.__file__` resolves under this checkout, not an
%% installed plugin cache) - not from reading the dict literals by eye.
%% `.crew/codemap/crew.md` agrees (its table was corrected from 114/63 to the
%% executed 116/65 on 2026-09-25) and carries the one-line re-measure command.
%% --
%% NOT re-verified this pass: no SVG/PNG was previously tracked for this
%% diagram (`git ls-files docs/diagrams/` shows only the .mmd); CONFIG.md's
%% per-key prose (sections 14-18) was spot-checked for section numbers only,
%% not read line-by-line against every claim below.
%% --
%% The one sentence this diagram exists to make visible, unchanged since the
%% previous anchor: what the global file may WRITE is exactly what the global
%% layer may SUPPLY, because both rules are enforced against
%% default_global_config(). The READ side prunes against it (filter_global);
%% the WRITE side refuses paths against it (plan_global_write). One
%% definition, two directions - and, new at this anchor, a THIRD thing now
%% reads a DIFFERENT repo file entirely for a different purpose (section 5).

flowchart TB

    subgraph Sources["Three sources, lowest precedence first"]
        direction TB
        DEF["default_config()<br/>plugin/crew/hooks/scripts/crew_config.py:239<br/><b>117 leaves</b> (executed, this pass)<br/>composed from crew_state's PM_DEFAULTS<br/>:1089, QA_DEFAULTS :1136, DEV_DEFAULTS<br/>:1145, WORKTREE_DEFAULTS :1172, CONTEXT_DEFAULTS<br/>:729, RESUME_DEFAULTS :692 (re-exported from crew_guards: INSTALL_DEFAULTS<br/>:47, GUARD_DEFAULTS :197, PRODUCTION_DEFAULTS<br/>:221, CLOUD_DEFAULTS :238 - repo-only), and<br/>crew_upgrade's GRAPH_BLOCK :48 / DOCS_BLOCK<br/>:107 / BITBUCKET_BLOCK :150 / GITHUB_BLOCK<br/>:173 / CHANGE_BLOCK :203"]
        GLB[("~/.claude/crew/config.json<br/>read_global_config()<br/>plugin/crew/hooks/scripts/crew_config.py:689<br/><i>never raises</i> - absent, malformed or<br/>non-object all return {}, because this is<br/>reached from a SessionStart hook")]
        REPO[(".crew/config.json - schema 7<br/>crew_state.load_config()<br/>plugin/crew/hooks/scripts/crew_state.py:261<br/>NOT the same file crew_context.py reads<br/>first - see the TwoFiles section below")]
    end

    subgraph Gate["The global layer is filtered BEFORE it is merged"]
        direction TB
        TMPL["default_global_config()<br/>plugin/crew/hooks/scripts/crew_config.py:372<br/><b>66 leaves</b> (executed, this pass) across<br/><b>15 blocks</b>: qa, dev, worktree, secondOpinion,<br/>memory (2 of 5 repo leaves), notify, pm,<br/>context.autoClear (8 of 9 - unsafeFocus is<br/>excluded, see AutoClear below), docs,<br/>bitbucket, github, install, guards (all TEN<br/>names), change - the single definition both<br/>rules below are enforced against"]
        PRUNE["_prune()<br/>plugin/crew/hooks/scripts/crew_config.py:621<br/>keeps only keys present in the template.<br/>Descends structurally, so naming a block<br/>grants only the leaves under it."]
        FILT["filter_global()<br/>plugin/crew/hooks/scripts/crew_config.py:647<br/>returns (kept, ignored)"]
    end

    subgraph Merge["resolve_config() - plugin/crew/hooks/scripts/crew_config.py:742 - the single resolver"]
        direction TB
        NULLS["without_null_shadows()<br/>plugin/crew/hooks/scripts/crew_config.py:598<br/>drops a repo <i>null</i> ONLY where the global<br/>layer supplies a real value."]
        M1["merge_defaults(default_config(), global)<br/>call: crew_config.py:775<br/>def: plugin/crew/hooks/scripts/crew_state.py:1322"]
        M2["merge_defaults(that, repo)<br/>call: crew_config.py:776"]
        SCHEMA{"schema<br/>lifted OUT of the merge<br/>crew_config.py:777-780"}
        OUT(["resolved config<br/><i>repo beats global beats default</i><br/>this is ONLY .crew/config.json's precedence -<br/>autoClear's own read path bypasses it, below"])
    end

    DEF --> M1
    GLB --> FILT
    TMPL -.->|"the shape to prune to"| PRUNE
    PRUNE --> FILT
    FILT -->|kept| M1
    FILT -.->|"ignored - named, never silent"| IGN>"keys a global file may not set"]
    REPO --> NULLS
    NULLS --> M2
    M1 --> M2
    M2 --> SCHEMA
    SCHEMA -->|"in repo file: use it"| OUT
    SCHEMA -->|"absent: pop it entirely"| OUT

    subgraph Write["The WRITE side - /crew:config --set"]
        direction TB
        PLAN["plan_global_write()<br/>plugin/crew/hooks/scripts/crew_config.py:2443<br/><i>pure</i>; returns (merged, changes)"]
        ISG{"is_global_path()<br/>crew_config.py:670, checked at :2469"}
        REFUSE>"GlobalWriteRefused (class :2091)<br/>names the path AND lists every allowed one"]
        VAL{"validate_providers()<br/>crew_config.py:175, checked at :2490"}
        PERR>"ProviderError (class :171)<br/>a VALUE check, not a path check"]
        MERGEW["merge, never replace - crew_config.py:2478<br/>keys the file already holds that<br/>the update does not name survive<br/>byte-for-byte"]
        WIDEN["_widens()<br/>plugin/crew/hooks/scripts/crew_config.py:2167<br/>called at :2524. RANK, not equality."]
        WRITE["write_global_config()<br/>plugin/crew/hooks/scripts/crew_config.py:2530<br/>the only function in crew that writes<br/>outside the repo. Sibling-then-rename<br/>with the PID in the tmp name, fsync<br/>before os.replace :2556-2563 - PRE-EXISTING<br/>since 0.16.7, not new at this anchor"]
    end

    PLAN --> ISG
    ISG -->|"not in the template"| REFUSE
    ISG -->|allowed| VAL
    VAL -->|"bad provider name"| PERR
    VAL -->|ok| MERGEW
    MERGEW --> WIDEN
    WIDEN --> WRITE
    WRITE --> GLB
    TMPL -.->|"the SAME object the read side prunes to"| ISG

    subgraph Ratchet["Two DIFFERENT ratchet tables, not one - 13 write-side, 12 read-side"]
        direction TB
        RG["<b>READ side: crew_guards.RATCHETED_KEYS</b><br/>plugin/crew/hooks/scripts/crew_guards.py:489-509<br/><b>12 keys</b> (executed + len()'d, this pass):<br/>install.policy, change.requireForProduction,<br/>and all TEN guards.* - terraformApply, forcePush,<br/>adminMerge, mergeGate, cloudDestructive,<br/>sqlDestructive, prodDatabase, prodServer,<br/>roleWrites, <b>cloudGuard (new)</b>.<br/>Consumed by crew_config.resolve_ratcheted() :784<br/>via crew_guards.effective_ratcheted() :524, called<br/>at crew_config.py:828"]
        RC["<b>WRITE side: crew_config._RATCHETED</b><br/>plugin/crew/hooks/scripts/crew_config.py:2351<br/>(extended through :2440)<br/><b>13 keys</b>: the same twelve PLUS pm.authority.<br/>pm.authority is still NOT in RATCHETED_KEYS -<br/>it resolves by ordinary precedence and only<br/>needs a widening NOTE when /crew:config writes it"]
        RSHAPE["Confirmed by import + diff + type(), not<br/>asserted: 12-key intersection, pm.authority the<br/>only difference (`set(_RATCHETED) -<br/>set(RATCHETED_KEYS) == {'pm.authority'}`), and<br/>the tuple field order STILL differs -- (function,<br/>function, dict) here, (tuple, function, function)<br/>there -- so the two still cannot be collapsed<br/>into one table as written"]
        RG --- RC --- RSHAPE
    end

    WIDEN -.-> Ratchet
    ISG -.->|"resolve_ratcheted / resolve_guard :981<br/>read the crew_guards table instead"| Ratchet

    subgraph Corrupt["A corrupt config file is its own state, and it BEATS the ratchet"]
        direction TB
        LS["crew_config.layer_state(path)<br/>plugin/crew/hooks/scripts/crew_config.py:873<br/>classifies ONE config file, either layer:<br/><b>absent</b> - nothing there (os.path.lexists false,<br/>so a DANGLING SYMLINK is corrupt, not absent).<br/><b>corrupt</b> - present but not the shape this key<br/>needs: unreadable, invalid JSON, not an object, or<br/>an object where <i>#quot;guards#quot; in parsed</i> and guards<br/>is not itself a dict (an EXPLICIT null is corrupt,<br/>not an unset key).<br/><b>ok</b> - everything else"]
        FORCE["plugin/crew/hooks/scripts/role_write_guard.py<br/>calls layer_state for BOTH layers at :760-761,<br/>ORs them into forced_by_corruption :762, and sets<br/>policy = #quot;block#quot; UNCONDITIONALLY at :764-765 --<br/>BEFORE the <i>elif policy == #quot;off#quot;: return 0</i><br/>early exit at :766-767"]
        LS --> FORCE
    end

    GLB -.-> LS
    REPO -.-> LS
    FORCE -.->|"overrides the resolved value"| Ratchet

    subgraph AutoClear["NEW at this anchor: context.autoClear's global-only keys and their writer"]
        direction TB
        ACREAD["crew_autocycle.settings(root)<br/>plugin/crew/hooks/scripts/crew_autocycle.py:137-182<br/>reads BOTH raw config files directly - this is<br/>NOT resolve_config, and never goes through it.<br/>enabled = machine.enabled is True AND<br/>repo.enabled is not False :175 - a repo may turn<br/>auto-clear OFF and never ON.<br/>onlyRepos/onlySessions read from the MACHINE<br/>layer only :177-178 - a repo's own copy is never<br/>consulted even though it is a settable leaf"]
        ACWRITE["crew_autoclear_setup.py<br/>write_autoclear_method() / write_autoclear_enabled()<br/>:239-269, both call<br/>_require_global_readable() :107-114 FIRST"]
        ACCHECK{"_global_file_problem()<br/>:69-104 - absent is fine (None);<br/>unreadable / not JSON / not an<br/>object is a named reason"}
        ACRAISE>"GlobalConfigUnreadable<br/>refuses to write over a malformed<br/>global file rather than replacing it"]
        ACGO["crew_config.write_global_config()<br/>(the SAME atomic writer the Write<br/>subgraph above uses - not a second<br/>mechanism)"]
    end

    GLB -.->|"read directly, bypassing filter_global/resolve_config"| ACREAD
    REPO -.->|"read directly"| ACREAD
    ACWRITE --> ACCHECK
    ACCHECK -->|malformed| ACRAISE
    ACCHECK -->|ok or absent| ACGO
    ACGO --> WRITE

    subgraph TwoFiles["OPEN AUTHORITY QUESTION - two modules read two different repo files as \"the config\""]
        direction TB
        CFGREAD["crew_config.py reads ONLY<br/>.crew/config.json (schema 7)<br/>module docstring :1; ratchet, guards,<br/>verify-gate.sh, /crew:config, /crew:model<br/>all route through this module"]
        CTXREAD["crew_context.load_crew_config()<br/>plugin/crew/hooks/scripts/crew_context.py:120-129<br/>tries .crew/crew.json FIRST,<br/>falls back to .crew/config.json<br/>only if crew.json is absent"]
        MIGRATE["crew_migrate.py --apply<br/>the ONLY writer of .crew/crew.json<br/>(schema 1). Never touches config.json's<br/>runtime read path - crew_config.py<br/>keeps reading config.json regardless"]
        OPEN["Net effect: a repo that has run<br/>--apply has BOTH files, and which one<br/>governs a given read depends on which<br/>module asked. Not resolved by this<br/>diagram - a decision for scribe to<br/>record, not this note's to make."]
        MIGRATE -.-> CTXREAD
        CFGREAD --- OPEN --- CTXREAD
    end

    REPO -.->|"the file crew_config.py reads"| CFGREAD

    subgraph NoPython["role-write-guard.sh's no-python fallback - a NARROWER contract than the old one"]
        direction TB
        NP1["_resolve_role_write_python()<br/>plugin/crew/hooks/scripts/role-write-guard.sh:44-<br/>fails to find a usable interpreter"]
        NP2["_role_write_fallback_decision()<br/>:326-346. Cannot evaluate guards.roleWrites<br/>(off/report/pm's own path allowances) at all -<br/>only tells a restricted role from an<br/>unrestricted one via the deny-list mirror"]
        NP3["Unrestricted role: allowed unjudged.<br/>Restricted role (pm or _DENY_ROLES) OR a role<br/>this fallback could not even read:<br/>ALWAYS BLOCKED (exit 2), regardless of what<br/>guards.roleWrites actually says :339-345"]
        NP1 --> NP2 --> NP3
    end

    subgraph Asym["The asymmetry, stated because 'exactly what' hides it"]
        direction TB
        A1["Both rules agree on which PATHS."]
        A2["The WRITE path additionally rejects VALUES<br/>the READ path only reports."]
        A3["resolve_config NEVER raises - a malformed file<br/>must not wedge every session on the machine."]
        A1 --- A2 --- A3
    end

    subgraph Split["What may be set where - measured, not asserted (executed this pass)"]
        direction TB
        S1["<b>66 global-settable</b> leaves, across fifteen<br/>blocks: qa, dev, worktree, secondOpinion, memory,<br/>notify, pm, context.autoClear, docs, bitbucket,<br/>github, install, guards (all TEN), change,<br/>resume (T-0006: machine opt-in, repo may only veto)"]
        S2["<b>51 repo-only</b> leaves (set difference, executed).<br/>Whole blocks absent from the global template:<br/>cloud, emergency, graph, jira, obsidian, platform,<br/>production, roles, schema, scope, sdp, tier,<br/>tracker, verifyGate - <b>cloud and scope are NEW<br/>repo-only blocks since the previous anchor</b><br/>(cloud identity pins, the T3 scope guard)"]
        S3["<b>Counts are executed, not read by eye.</b><br/>117 repo-template / 66 global / 51 repo-only at<br/>6d35ef8c (116 / 65 / 51 at 6c497a14); .crew/codemap/crew.md agrees and gives<br/>the re-measure command."]
        S4["<b>Consent is not capability.</b><br/>context.autoClear.unsafeFocus is excluded from the<br/>global template on purpose (AUTOCLEAR_CONSENT_KEYS,<br/>crew_state.py:705) - a Wayland focus-typing risk<br/>accepted once must not arm for every repo."]
        S1 --- S2 --- S3 --- S4
    end

    OUT -.-> Split

    subgraph Except["Twelve keys do NOT follow the precedence above (read-side ratchet)"]
        direction TB
        E1["<b>Every key in crew_guards.RATCHETED_KEYS :489-509 --<br/>install.policy, change.requireForProduction and all<br/>TEN guards.* -- resolves by the LOWER rank, not<br/>by repo-beats-global.</b> CONFIG.md section 15 covers<br/>install.policy, section 16 the guards, section 18<br/>guards.roleWrites."]
        E2["The effective value at each key is the <b>lower-ranked<br/>of the two layers</b> - crew_guards.effective_ratcheted<br/>:524, reached through crew_config.resolve_ratcheted<br/>:775."]
        E3["Why: crew reads config out of CLONED repositories.<br/>A repo shipping install.policy: auto, or<br/>guards.cloudGuard: report, would, under precedence,<br/>override a machine owner who chose manual or off."]
        E4["Absent counts as the narrowest tier on either side<br/>for TEN of the twelve, so the WIDEST grant requires<br/>BOTH layers to agree."]
        E5["<b>guards.roleWrites and guards.cloudGuard are the two<br/>exceptions to E4, and the only keys here whose<br/>DEFAULT is not their FLOOR.</b> ROLE_WRITE_POLICIES is<br/>(block, report, off); ROLE_WRITE_DEFAULT is <b>off</b>,<br/>the WIDEST tier, for both names - cloudGuard reuses<br/>roleWrites' own normalise/rank functions rather than<br/>growing a second copy (crew_guards.py:417-454,<br/>guard_tiers() :457-471)."]
        E6["install.policy: auto is safe only because no<br/>repo-controlled string can become a command -<br/>crew_guards.INSTALLABLE maps a name to a literal<br/>argv tuple, consulted BEFORE the policy. Asserted in<br/>tests/test_install_policy.py."]
        E1 --- E2 --- E3 --- E4 --- E5 --- E6
    end

    OUT -.-> Except
    WIDEN -.->|"13 keys ratchet on write (E1's 12,<br/>plus pm.authority for the widening notice only)"| Except
